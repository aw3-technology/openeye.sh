---
title: GET /v1/usage
---

> [!warning] The hosted API endpoint is not yet available. This page describes planned functionality.

### Self-Hosted Alternative

When running `openeye serve`, the server logs inference requests with timing information. Use the `/health` endpoint to check server status:

```bash
curl http://localhost:8000/health
```
